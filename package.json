{
  "name": "warehouse-practice",
  "version": "1.0.0",
  "private": true,
  "description": "React TypeScript + NestJS TypeScript + MySQL practice project",
  "scripts": {
    "postinstall": "npm install --prefix frontend --no-audit --no-fund && npm install --prefix backend --no-audit --no-fund",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f mysql",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test:backend": "npm run test --prefix backend -- --runInBand"
  }
}
